label: Blog Generator
icon: edit
description: Draft a fact-checked winner profile article on demand, in the Stevie blog's own style.
value:
  # Must match the kebab path under src/components/ — renderBlock.astro resolves
  # the component by this exact key.
  _component: page-sections/features/blog-generator
  heading: "Generate a Winner Profile"
  description: "Pick a Stevie Award winner, optionally say what the piece should be about, and draft a fact-checked article in the Stevie blog's own style."
  apiEndpoint: ""
  winnerProfiles:
    - "org::IBM Corporation"
  emptyStateText: "Waiting for the first saved pick to be drafted."
  debug: true
  colorScheme: inherit
  backgroundColor: base
preview:
  text:
    - Blog Generator
  subtext:
    - key: heading
  icon: edit
picker_preview:
  text: Blog Generator
  subtext: Draft a fact-checked winner profile article on demand, in the Stevie blog's own style.
_inputs_from_glob:
  - /src/components/page-sections/features/blog-generator/blog-generator.cloudcannon.inputs.yml
