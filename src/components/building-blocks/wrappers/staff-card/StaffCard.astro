---
import Button from "@core-elements/button/Button.astro";
import Heading from "@core-elements/heading/Heading.astro";
import Image from "@core-elements/image/Image.astro";
import Text from "@core-elements/text/Text.astro";
import Card from "@wrappers/card/Card.astro";
import Split from "@wrappers/split/Split.astro";

const {
  photo = { source: "", alt: "" },
  heading = "",
  jobTitle = "",
  description = "",
  colorScheme = "inherit",
  backgroundColor = "surface",
  class: className,
  _component,
  ...htmlAttributes
} = Astro.props;

// Derived from the name, this collapsed to a run of hyphens for any name written
// in a non-Latin script — every character failed [a-z0-9] — so two names of equal
// length produced the same id. That is a duplicate-id-aria failure, and worse, it
// broke the control: <label for> binds to the first match, so "Read More" on one
// card expanded a different person's biography on every non-Latin locale.
const uid = `staff-bio-${crypto.randomUUID()}`;
---

<Card
  colorScheme={colorScheme}
  backgroundColor={backgroundColor}
  class:list={["staff-card", className]}
  {...htmlAttributes}
>
  <Split
    distributionMode="fixed-flexible"
    fixedWidth={180}
    verticalAlignment="top"
    minSplitWidth={300}
    gap="md"
  >
    <Fragment slot="first">
      <Image
        source={photo?.source || ""}
        alt={photo?.alt || heading}
        data-prop-src="photo.source"
        data-prop-alt="photo.alt"
      />
    </Fragment>
    <Fragment slot="second">
      <div class="staff-card-content">
        <!-- h2, not h3: these cards sit directly under the page <h1>, so h3 skips a
             level. h2 is also safe under a section h2 — heading-order only fails on
             skips, not on repeats. -->
        <Heading
          level="h2"
          size="md"
          text={heading}
          data-prop="heading"
        />
        <Text
          class="staff-card-job-title"
          data-prop="jobTitle"
          size="sm"
        >
          {jobTitle}
        </Text>
        <div class="staff-card-bio-wrapper">
          <input
            type="checkbox"
            id={uid}
            class="staff-card-toggle"
          />
          <Text
            text={description}
            data-prop="description"
            class="staff-card-description"
          />
          <label
            for={uid}
            class="staff-card-read-more"
          >
            <Button
              class="read-more-label"
              variant="text-brand-muted"
              size="sm"
              uppercase={false}
              useDefaultEditableBinding={false}
              >Read More</Button
            >
            <Button
              class="read-less-label"
              variant="text-brand-muted"
              size="sm"
              uppercase={false}
              useDefaultEditableBinding={false}
              >Read Less</Button
            >
          </label>
        </div>
      </div>
    </Fragment>
  </Split>
</Card>

<style is:global>
  @layer building-blocks {
    .staff-card .split .split-inner {
      gap: 0;
    }

    .staff-card .image,
    .staff-card .image .image-inner,
    .staff-card .image picture {
      height: 100%;
      width: 100%;
    }

    .staff-card .image img {
      height: 100%;
      object-fit: cover;
      object-position: center top;
      width: 100%;
    }

    .staff-card-content {
      padding: var(--spacing-lg);
    }

    .staff-card-content .heading {
      text-transform: uppercase;
      margin: 0;
    }

    .staff-card-job-title {
      margin: 0;
      color: var(--color-text);

      /* 0.6 composited to #767c85 on the card surface — 4.01:1, under the 4.5:1
         minimum. 0.7 keeps the muted look and clears it on every surface the card
         is used on (light/dark × bg, bg-surface, bg-muted): lowest measured 4.99:1. */
      opacity: 0.7;
    }

    .staff-card-bio-wrapper {
      margin-top: var(--spacing-sm);
      position: relative;
    }

    .staff-card-toggle {
      clip: rect(0 0 0 0);
      clip-path: inset(50%);
      height: 1px;
      overflow: hidden;
      position: absolute;
      white-space: nowrap;
      width: 1px;
    }

    .staff-card-description .text-inner {
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 4;
      overflow: hidden;
    }

    .staff-card-description .text-inner p {
      margin: 0;
    }

    .staff-card-toggle:checked ~ .staff-card-description .text-inner {
      display: block;
      overflow: visible;
    }

    .staff-card-read-more {
      display: inline-block;
    }

    .staff-card-read-more .read-less-label {
      display: none;
    }

    .staff-card-toggle:checked ~ .staff-card-read-more .read-more-label {
      display: none;
    }

    .staff-card-toggle:checked ~ .staff-card-read-more .read-less-label {
      display: inline;
    }
  }
</style>
