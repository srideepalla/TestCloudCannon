{
  "name": "stevie-profiles-api",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Cloudflare Worker that accepts profile JSON over HTTP and commits it to the profiles content collection on GitHub.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
